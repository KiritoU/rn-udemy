import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  input: {
    borderBottomColor: 'gray',
    borderBottomWidth: 2,
    width: 50,
    marginVertical: 20,
    textAlign: 'center',
  },
});
